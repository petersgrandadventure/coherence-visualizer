# monitor data — never commit a ledger
*.db
*.db-journal
*.csv

# python
__pycache__/
*.pyc
venv/

# os
.DS_Store

# upstream monitor lives here untracked (its own project + venv + data)
monitor/
.DS_Store
*.log
reg/accel_stream
